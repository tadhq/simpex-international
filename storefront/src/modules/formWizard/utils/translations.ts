// TODO this should be removed once migrated
import en from '../../../../locales/en.json';

// HACK this translation is static, we leave here for compatibility
export const getTranslations = () => en;
